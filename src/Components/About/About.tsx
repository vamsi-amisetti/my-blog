import React, { useEffect, useRef, useState } from 'react';
import './About.css';

interface Props {
}

const About: React.FC<Props> = ({ }) => {
    return(
        <div>
            About
        </div>
    );
};

export default About;