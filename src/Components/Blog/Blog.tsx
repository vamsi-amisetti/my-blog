import React, { useEffect, useRef, useState } from 'react';
import './Blog.css';

interface Props {
}

const Blog: React.FC<Props> = ({ }) => {
    return(
        <div>
            Blog
        </div>
    );
};

export default Blog;