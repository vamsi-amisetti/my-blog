import React, { useEffect, useRef, useState } from 'react';
import './Contact.css';

interface Props {
}

const Contact: React.FC<Props> = ({ }) => {
    return(
        <div>
            Contact
        </div>
    );
};

export default Contact;